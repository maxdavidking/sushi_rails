<h1> User Profile </h1>

<%= link_to 'Edit Profile', edit_user_path(current_user.id) %>

<ul>
  <label>
    Name:
    <%= current_user.name %>
  </label>
  <label>
    Organization:
    <%= current_user.organization %>
  </label>
</ul>
