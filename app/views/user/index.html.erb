<h1> User Profiles </h1>


<% @user.each do |u| %>
  <li>
    <%= u.id %>
    <%= u.name %>
    <%= u.organization %>
    <%= link_to 'Edit Profile', edit_user_path(u.id) %>
  </li>
  <% end %>
</ul>
